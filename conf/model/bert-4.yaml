name: BERT-4

encoder:
  unit: BERT
  should_freeze: true
  
decoder:
  unit: Transformer
  type: sequence
  dropout: 0
  num_layers: 4
  max_length: 30
  hidden_size: 768
  embedding_size: 768
  num_heads: 4
  attention: null
  